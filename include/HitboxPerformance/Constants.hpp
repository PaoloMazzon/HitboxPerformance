#include <cstdint>
#pragma once

namespace Constants {
    inline constexpr uint32_t WINDOW_WIDTH = 800;
    inline constexpr uint32_t WINDOW_HEIGHT = 600;
    inline constexpr const char *WINDOW_TITLE = "Hitbox Performance";
    inline constexpr double TARGET_FPS = 60;
    inline constexpr float HITBOX_SIZE = 32;
    inline constexpr float MAX_SPEED = 3;
    inline constexpr uint32_t HITBOX_COUNT = 10;
}